<view>
  <zan-tab list="{{ tab.list }}" selected-id="{{ tab.selectedId }}" />
  <scroll-view scroll-y="true" lower-threshold="200" style="height:{{ winH }}px;" bindscrolltolower="loadMore" bindscrolltoupper="refesh">
    <!-- <view style='margin-top:0px'></view> -->
    <block wx:for="{{currencies}}" wx:for-item="currency" wx:key="{{currency.id}}">
      <view bindtap="open" data-currency="{{currency}}">
        <zan-panel without-border>
          <zan-card isPercentChangePositive="{{ currency.isPercentChangePositive }}
          card-class=" test-card zan-card__thumb2 "
          thumb="{{ coin_img_url }}/{{ currency.symbol }}.png "
          price="${{ currency.quotesUSDPrice }} "
          title="{{ currency.name }} ({{ currency.symbol }}) "
          rank="{{ currency.quotesUSDPercentChange24h }} "
          desc="{{ labelMarketCap }} #{{ currency.rank }} ${{ currency.quotesUSDMarketCap }} "
          status=" ">
          </zan-card>
        </zan-panel>
      </view>
    </block>
    <view wx:if="{{ hasMore }}">
      <zan-loading type="spinner" class="loading-stage" color="black" style="width:100%;"></zan-loading>
    </view>
    <!-- <view class="">
      <view wx:if="{{hasMore}}" style="display: flex;flex-direction: row;align-items: center;align-self: center;justify-content: center;">
      <icon type="waiting" size="45"/><text>玩命的加载中...</text></view>
      <view wx:else><text>没有更多内容了</text></view>
    </view> -->
  </scroll-view>

  <!-- <view style='margin-top:0px'></view>
  <block wx:for="{{currencies}} " wx:for-item="currency " wx:key="{{currency.id}} ">
    <view bindtap="open " data-currency="{{currency}} ">
      <zan-panel without-border>
        <zan-card
          isPercentChangePositive="{{ currency.isPercentChangePositive }} card-class="test-card zan-card__thumb2" thumb="{{ coin_img_url }}/{{ currency.symbol }}.png" price="${{ currency.quotesUSDPrice }}" title="{{ currency.name }} ({{ currency.symbol }})"
            rank="{{ currency.quotesUSDPercentChange24h }}" desc="{{ labelMarketCap }} #{{ currency.rank }} ${{ currency.quotesUSDMarketCap }}" status="">
          </zan-card>
        </zan-panel>
      </view>
    </block> -->
</view>